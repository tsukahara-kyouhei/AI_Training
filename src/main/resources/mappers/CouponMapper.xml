<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespaceも正しいMapperのパスに修正しています -->
<mapper namespace="jp.co.skig.officeorder.mapper.CouponMapper">

    <!-- resultTypeを正しいCouponクラスのパスに修正 -->
    <select id="findByCode" resultType="jp.co.skig.officeorder.model.coupon.Coupon">
        SELECT *
        FROM coupons
        WHERE coupon_code = #{code}
    </select>

    <select id="countUsage" resultType="int">
        SELECT COALESCE(SUM(usage_count), 0)
        FROM member_coupon_usage
        WHERE member_id = #{memberId}
          AND coupon_id = #{couponId}
    </select>

    <!-- 引数のusageプロパティから値を取得してINSERT -->
    <insert id="insertUsage">
        INSERT INTO member_coupon_usage (
            member_id,
            coupon_id,
            usage_count,
            last_used_at,
            created_at,
            updated_at
        ) VALUES (
            #{usage.memberId},
            #{usage.couponId},
            #{usage.usageCount},
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP
        )
    </insert>

</mapper>