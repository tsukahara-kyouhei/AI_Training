<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CouponMapper">

    <!-- クーポンコードでクーポン情報を取得 -->
    <select id="findByCode" parameterType="String" resultType="com.example.model.Coupon">
        SELECT *
        FROM coupons
        WHERE coupon_code = #{code}
    </select>

    <!-- 会員のクーポン使用回数を取得 -->
    <select id="countUsage" parameterType="map" resultType="int">
        SELECT COALESCE(SUM(usage_count), 0)
        FROM member_coupon_usage
        WHERE member_id = #{memberId}
          AND coupon_id = #{couponId}
    </select>

    <!-- 会員のクーポン使用履歴を登録 -->
    <insert id="insertUsage" parameterType="map">
        INSERT INTO member_coupon_usage
        (member_id, coupon_id, usage_count, last_used_at, created_at, updated_at)
        VALUES
        (#{memberId}, #{couponId}, 1, #{lastUsedAt}, #{createdAt}, #{updatedAt})
    </insert>

</mapper>
